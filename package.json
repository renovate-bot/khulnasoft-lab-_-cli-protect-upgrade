{
  "name": "cli-protect-upgrade",
  "bin": {
    "cli-protect-upgrade": "bin/cli-protect-upgrade"
  },
  "files": [
    "bin",
    "index.js"
  ],
  "description": "Upgrade `khulnasoft protect` into the new `protect`",
  "version": "1.0.0",
  "author": "khulnasoft.com",
  "license": "Apache-2.0",
  "scripts": {
    "test": "jest",
    "format": "prettier --write '**/*.{js,ts,json,yaml,yml,md}'",
    "lint:formatting": "prettier --check '**/*.{js,ts,json,yaml,yml,md}'"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/khulnasoft-lab/cli-protect-upgrade.git"
  },
  "devDependencies": {
    "@types/fs-extra": "^9.0.12",
    "@types/jest": "^30.0.0",
    "fs-extra": "^10.0.0",
    "jest": "^30.0.0",
    "prettier": "^2.3.2",
    "ts-jest": "^29.0.0",
    "typescript": "^4.4.2"
  }
}
